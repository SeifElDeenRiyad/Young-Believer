<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    android:layoutDirection="rtl"
    android:orientation="vertical">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/white"
        android:text="@string/hag"
        android:padding="7dp"
        android:gravity="start"
        android:textSize="33sp"
        android:textStyle="bold"
        android:id="@+id/hag"/>

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scaleType="centerCrop"
        android:src="@drawable/hag"
        android:id="@+id/hag_image"
        android:contentDescription="@string/hag_pillar_image" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight=".8"
        android:fillViewport="true"
        android:id="@+id/arkan_hajj_scroll">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/white"
            android:text="@string/hag_info"
            android:padding="7dp"
            android:textAlignment="viewStart"
            android:textSize="20sp"
            android:id="@+id/hag_info"/>
    </ScrollView>
</LinearLayout>